# Claude memory & per-machine state
.claude/settings.local.json
.claude/worktrees/
.claude/scheduled_tasks.lock

# Local-only drafts
community-posts/

# Logs at repo root
*.log

# OS
.DS_Store
Thumbs.db

# Leftover build artifacts/caches at repo root from pre-reorganization layout
# (node_modules, dist, dist-python, build-python, python-core/.venv etc.)
# Can be safely deleted — regenerate by running npm install / build from tarkov-price-overlay/
node_modules/
dist/
dist-python/
build-python/
python-core/
src-tauri/
